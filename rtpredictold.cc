//James Dalgleish
//C++ retention time predictor-- RTPredict
//basic file i/o may resemble tutorial code on: http://www.cprogramming.com/tutorial/c/lesson14.html
//hrlog 11:46-12:00, 1:16-1:48
#include<iostream>
#include<stdio.h>
#include<ctype.h>
#include<string.h>
int main (int argc, char *argv[]) {
	if (argc = 2)
	{
		//FILE *file = fopen(argv[1],"w+");
		//printf(file);
         //add mzxml input here
         /*
         switch (substr(argv[1].rfind(".")).tolower()) //switching for extensions
         {
         case 'mzml'
         {

         }
         }

	}
	else
	{
		printf("usage RTPredict \<inputfile.correctextension\>");
	}
	return 0;
	}
	*/	}
//const char* blah = std::string();
//printf("%d",int(str1.rfind (".",1)));
std::string filenamewithext(argv[1]);
return 0;
}
